import { describe,expect,it } from 'vitest'
import { flattenComments,mapAttraction,mapBoard,mapFestival,mapPost } from './mappers'
describe('API mappers',()=>{
  it('normalizes localized content and supplies the tourism fallback image',()=>{const item=mapAttraction({contentId:'a',name:'해운대',category:'BEACH',summary:'요약',description:'설명',image:'',address:'부산',boardId:3});expect(item.name).toEqual({ko:'해운대',en:undefined});expect(item.boardId).toBe('3');expect(item.image).toBe('/images/busan-placeholder.png')})
  it('normalizes statuses, board ids, and structured board descriptions',()=>{expect(mapFestival({contentId:'f',name:'축제',status:'UPCOMING',place:'부산',period:'7월',image:'',summary:'요약',boardId:4}).status).toBe('upcoming');const board=mapBoard({boardId:2,name:'장소',category:'ATTRACTION',description:'주소: 부산광역시 동구 | 우편번호: 48808'});expect(board.id).toBe('2');expect(board.description.ko).toBe('주소: 부산광역시 동구 · 우편번호: 48808')})
  it('keeps media and tags while shortening anonymous UUIDs',()=>{const item=mapPost({postId:1,boardId:2,title:'제목',author:'12345678-1234-5678-9012-123456789012',content:'본문',viewCount:1,likeCount:2,commentCount:3,createdAt:'2026-01-01T00:00:00Z',updatedAt:null,tags:[{tagId:1,name:'후기',category:'REVIEW'}],media:[{mediaId:7,imageUrl:'/a.jpg'}]});expect(item.author).toBe('12345678');expect(item.tags[0].kind).toBe('review');expect(item.media[0].id).toBe(7)})
  it('flattens recursive comments without limiting depth',()=>{const comments=flattenComments([{commentId:1,postId:2,parentId:null,author:'12345678-aaaa',content:'1',contentKr:'댓글',contentEn:'Comment',createdAt:null,updatedAt:null,children:[{commentId:2,postId:2,parentId:1,author:'b',content:'2',createdAt:null,updatedAt:null,children:[{commentId:3,postId:2,parentId:2,author:'c',content:'3',createdAt:null,updatedAt:null}]}]}]);expect(comments.map(x=>x.parentId)).toEqual([undefined,'1','2']);expect(comments[0].author).toBe('12345678');expect(comments[0].body).toBe('댓글');expect(comments[0].bodyEn).toBe('Comment')})
})
